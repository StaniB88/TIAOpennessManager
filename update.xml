<?xml version="1.0" encoding="UTF-8"?>
<item>
    <version>3.0.11</version>
    <url>https://github.com/StaniB88/TIAOpenessManager/releases/download/v3.0.11/TiaOpennessManager_Setup_3.0.11.exe</url>
    <changelog>https://github.com/StaniB88/TIAOpenessManager/blob/main/CHANGELOG.md</changelog>
    <release_date>2026-04-09</release_date>
    <body>## What&apos;s Changed

### Improvements
- Error and warning messages in the Application Log are now color-coded (red for errors, amber for warnings) for easier identification
- **File Explorer: Git Status** â€” Opening a folder that contains a Git repository now shows file status directly in the tree: modified files appear yellow (M), new files green (U), deleted red (D). Folders show a small colored dot when they contain changed files. Status updates automatically after commits, checkouts, or other Git operations

### Import
- **Transaction Rollback on Error** â€” When &quot;Fault Tolerant&quot; is disabled in settings, import errors now trigger an automatic transaction rollback, restoring the project to its original state instead of leaving it in an inconsistent state
- **Fault Tolerant Mode** â€” The &quot;Fault Tolerant&quot; checkbox now controls error behavior: disabled = abort and rollback on first error, enabled = continue importing remaining files

### AI Chat â€” Skills
- **Skills write to project folder** â€” Analysis results, documentation, and reports are now saved directly into the TIA Portal project folder instead of cluttering the chat with hundreds of lines of text
- **One-click skill execution** â€” All skills now auto-send when clicked, matching the behavior of slash commands
- **Automatic sub-agent dispatch** â€” Skills automatically route tasks to the best specialized AI agent (e.g., SCL Expert for code generation, TIA Analyzer for documentation)
- **Skill version management** â€” Built-in skills update automatically when a new app version includes improved skill prompts

### AI Chat â€” Sub-Agents
- **Full tool access** â€” All sub-agents now have access to all available tools, preventing &quot;missing tool&quot; errors during complex tasks
- **Mandatory agent routing** â€” The AI reliably delegates specialized tasks (SCL code, block analysis, AWL conversion) to the appropriate sub-agent instead of attempting them directly
- **SCL Expert: UDT support** â€” The SCL Expert now creates UDTs for complex data structures instead of monolithic blocks with dozens of flat variables. UDTs are generated first, then blocks that reference them
- **SCL Expert: Correct block structure** â€” Generated blocks now follow the exact TIA Portal import format. Block header comments are placed inside the BEGIN section where TIA Portal preserves them

### AI Chat â€” Steering
- **Send messages during generation** â€” You can now type and send messages while the AI is working. The Send button and Stop button appear side by side. Press Enter to steer the AI or add instructions without stopping the current generation

### Code Editor
- **Improved create templates** â€” New FB and FC templates now include all VAR sections (VAR_INPUT, VAR_OUTPUT, VAR_IN_OUT, VAR, VAR_TEMP, VAR CONSTANT)

### Bug Fixes
- Fixed skills showing full prompt text in chat instead of executing silently
- Fixed skill invocations in one session leaking into other concurrent sessions
- Fixed &quot;Describe Project&quot; overwriting its own generated documentation file with a short summary
- Fixed sub-agents not receiving per-agent instruction files (e.g., Siemens Programming Styleguide for SCL Expert)
- Fixed Send button and Enter key not working during AI generation â€” steering messages can now be sent at any time
- Fixed Import All failing with &quot;object already exists&quot; error when the default tag table had a different filename than its block name (e.g., Safety Unit default tag tables)
- Fixed operations (Export, Import, Find Unused, etc.) not starting again after cancelling â€” previously required an app restart
- Fixed editor minimap staying visible with stale content after pressing Clear
- Fixed TIA Portal connection showing false warning messages in the log on first Connect
- Fixed Inline Chat (Ctrl+I) not opening â€” the shortcut now works reliably regardless of editor focus state
- Fixed syntax highlighting not working in the Code Editor tab
- Fixed AI Chat tool categories (Visual Context, Agent Memory, Skills) missing from Settings â€” toggling them in Settings now works the same as in the chat dropdown
- Fixed AI tools found via &quot;Search Tools&quot; failing with &quot;Function not found&quot; when the AI tried to use them in the same response</body>
    <mandatory>false</mandatory>
</item>
